[package]
name = "xtask"
version = "0.1.0"
edition.workspace = true
license.workspace = true
publish = false

# Deliberately dependency-free: this is dev tooling in the same repo as a Zig
# project, and a zero-dependency build keeps it fast and auditable.
[dependencies]
